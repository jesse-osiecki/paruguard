# Test fixture for PLAN.md §11 test 5 — --noscriptlet proven (I4).
# post_install writes a marker file; after `paruguard -S`, it must NOT exist.
pkgname=paruguard-test-noscriptlet
pkgver=1.0
pkgrel=1
pkgdesc="paruguard test fixture: post_install marker that must never run"
arch=('any')
url="https://example.invalid"
license=('MIT')
install=paruguard-test-noscriptlet.install

package() {
	mkdir -p "$pkgdir/usr/share/$pkgname"
}
